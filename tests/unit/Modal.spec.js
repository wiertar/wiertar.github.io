import { expect } from 'chai';
import { shallowMount } from '@vue/test-utils';
import Modal from '@/components/Modal.vue';

describe('Modal.vue', () => {
    it('shows a modal when display is set to true', (done) => {
        const wrapper = shallowMount(Modal);
        wrapper.vm.$
    });
});